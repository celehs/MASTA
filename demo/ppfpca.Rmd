---
title: "PPFPCA"
---

```{r}
library(PETLER)
```

```{r}
TrainCode <- data.frame(data_org$TrainCode)
TrainN <- rowsum(TrainCode[, -(1:3)], group = TrainCode[, 1])
codes <- colnames(TrainN)
```

```{r}
i <- 1

TrainNP <- TrainN[TrainN[, i + 1], i + 1]
id <- rep(id, TrainN[, i + 1])
```

```{r}
ppfpca.kern <- function(time, h1 = NULL, h2 = NULL) {
    
}
```
